import "../../styles/style.scss";
import { initCommon } from "./common";

// サービスページ用の初期化
document.addEventListener("DOMContentLoaded", () => {
  // 全ページ共通処理
  initCommon();

  // 将来、固有ページ専用の処理をここに追加
});
